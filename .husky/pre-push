#!/usr/bin/env sh

echo "🔍 Running pre-push checks..."

# Run basic linting (allows warnings)
cd client && npm run lint
if [ $? -ne 0 ]; then
  echo "❌ Linting failed. Please fix the errors before pushing."
  exit 1
fi

echo "✅ Pre-push checks passed!"
