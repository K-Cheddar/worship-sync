/** Initial tile count before scrolling (panel + fullscreen modal). */
export const MEDIA_GRID_PROGRESSIVE_INITIAL = 36;

/** How many additional tiles to mount when near the bottom of the scroll container. */
export const MEDIA_GRID_PROGRESSIVE_BATCH = 24;

/** Pixels from bottom of the scroll container to trigger the next batch (matches song list intent). */
export const MEDIA_GRID_LOAD_THRESHOLD_PX = 280;
